# Flow 03: Lab-only scan queue

1. Add your lab host to `config/allowlist.txt`.
2. Import or register URLs.
3. Queue a profile with `scan-plan`.
4. Run checks with `scan-run`.
5. Review `scan-results` and `scan-findings`.
